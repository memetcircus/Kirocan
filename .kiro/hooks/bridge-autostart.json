{
  "version": "v1",
  "hooks": [
    {
      "name": "Auto-start KiroCan Bridge",
      "trigger": "SessionStart",
      "description": "Starts the KiroCan bridge if not already running",
      "action": {
        "type": "command",
        "command": "cmd /c \"curl -s http://localhost:9848/health >nul 2>&1 || (cd /d bridge && start /B node dist\\index.js)\"",
        "timeout": 10
      }
    }
  ]
}
