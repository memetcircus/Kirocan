{
  "version": "v1",
  "hooks": [{
    "name": "KiroCan Idle Signal",
    "trigger": "Stop",
    "action": {
      "type": "command",
      "command": "curl -s --max-time 2 -X POST http://localhost:9848/hook/idle"
    }
  }]
}
